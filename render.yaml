services:
  - type: web
    name: quick-odds-uganda
    runtime: node
    buildCommand: npm install && npm install --prefix backend && npm run build
    startCommand: node backend/src/server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
